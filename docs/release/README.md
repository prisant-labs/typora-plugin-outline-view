# Release Documentation

This directory holds public, versioned release documentation such as release
checklists and marketplace submission notes. Keep machine-specific staging
files and release credentials under the ignored `_local/release-staging/`
directory instead.
