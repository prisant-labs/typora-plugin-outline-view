# Outline View 0.3.1

**Release date:** September 23, 2026

This patch makes plugin identity and update context visible without taking space
from the settings controls. The compact masthead appears above section navigation
and shows author, installed version, latest published version, last release date,
GitHub, and a small Local folder button, in that order. A middle dot separates
the link from the button. The masthead stays inside the native settings modal
and remains visible when navigating sections, including narrow layouts.

Local folder opens the installed plugin directory through the Community Plugin
app API. Current and Last updated come from the latest published GitHub release;
when that lookup is unavailable, the masthead says so and settings still work.
The header does not include a plugin description, release-notes link, or issue
link. The [offline settings reference](../prototype/settings.html) uses simulated
release and folder services and is not a live update check.

There are no settings migrations or outline behavior changes. Community Plugin
2.10.21 or later remains required. Windows and macOS remain the advertised
platforms; Linux runtime support is not claimed. The manifest's Typora 1.4.0
minimum remains independently uncertified.

## Validation

On September 23, 2026, the maintainer confirmed native testing on macOS and
Windows and authorized publication. This is maintainer-reported validation;
exact platform versions, themes, and per-case observations were not supplied.
The [native validation record](NATIVE-CHECKLIST.md) identifies the preserved
candidate and records the earlier native issues separately.

Automated coverage includes 204 tests across 26 files, two Chromium layout tests,
typecheck, prototype freshness, package validation, dependency audit, and required
Windows/Linux CI. Browser and CI results are separate from native Typora testing.

## Installation

Update through Marketplace after its metadata refreshes, or follow the
[manual installation instructions](../../README.md#manual-installation-or-candidate-testing)
using [0.3.1's plugin.zip](https://github.com/prisant-labs/typora-plugin-outline-view/releases/download/0.3.1/plugin.zip).
The existing marketplace entry already points to this repository, so this update
does not require another registry PR. GitHub's source archive is not a plugin package.

The published `plugin.zip` preserves the validated candidate's bytes (27,317 bytes).
Its SHA-256 is:

```text
07bae5905548194d08a9a1440cdd64ea9ddf54c7aeedef105846920c2bf2f457
```
